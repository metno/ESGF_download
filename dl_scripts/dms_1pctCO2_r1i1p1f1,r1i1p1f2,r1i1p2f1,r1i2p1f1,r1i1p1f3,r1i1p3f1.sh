#!/bin/bash
##############################################################################
# ESG Federation download script
#
# Template version: 1.2
# Generated by esgf-data.dkrz.de - 2019/10/11 15:08:43
# Search URL: https://esgf-data.dkrz.de/esg-search/wget?mip_era=CMIP6&variable=dms&experiment_id=1pctCO2&variant_label=r1i1p1f1,r1i1p1f2,r1i1p2f1,r1i2p1f1,r1i1p1f3,r1i1p3f1&realm=aerosol,atmos,atmosChem,ocean,seaIce&frequency=mon,fx&limit=9999&download_structure=experiment_id,source_id,variant_label
#
###############################################################################
# first be sure it's bash... anything out of bash or sh will break
# and the test will assure we are not using sh instead of bash
if [ $BASH ] && [ `basename $BASH` != bash ]; then
    echo "######## This is a bash script! ##############" 
    echo "Change the execution bit 'chmod u+x $0' or start with 'bash $0' instead of sh."
    echo "Trying to recover automatically..."
    sleep 1
    /bin/bash $0 $@
    exit $?
fi

version=1.3.2
CACHE_FILE=.$(basename $0).status
openId=
search_url='https://esgf-data.dkrz.de/esg-search/wget?mip_era=CMIP6&variable=dms&experiment_id=1pctCO2&variant_label=r1i1p1f1,r1i1p1f2,r1i1p2f1,r1i2p1f1,r1i1p1f3,r1i1p3f1&realm=aerosol,atmos,atmosChem,ocean,seaIce&frequency=mon,fx&limit=9999&download_structure=experiment_id,source_id,variant_label'

#These are the embedded files to be downloaded
download_files="$(cat <<EOF--dataset.file.url.chksum_type.chksum
'1pctCO2/BCC-ESM1/r1i1p1f1/dms_AERmon_BCC-ESM1_1pctCO2_r1i1p1f1_gn_185001-200012.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/BCC/BCC-ESM1/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190718/dms_AERmon_BCC-ESM1_1pctCO2_r1i1p1f1_gn_185001-200012.nc' 'SHA256' '476f842b4e6f6f999f4c95107cd977359f6c1e9fd4247c182945fb38a745a43a'
'1pctCO2/MIROC-ES2L/r1i1p1f2/dms_AERmon_MIROC-ES2L_1pctCO2_r1i1p1f2_gn_185001-199912.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/MIROC/MIROC-ES2L/1pctCO2/r1i1p1f2/AERmon/dms/gn/v20190823/dms_AERmon_MIROC-ES2L_1pctCO2_r1i1p1f2_gn_185001-199912.nc' 'SHA256' '0c788e28318671af39af7dc4bb5088ed648b7ae2bf2c0f185a31396337744fd0'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_320001-320912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_320001-320912.nc' 'SHA256' '379ca5bda405bb2e0ee2824617df65480486d5d45ff18c6c34ce8a6e90596df8'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_321001-321912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_321001-321912.nc' 'SHA256' '56affbeff685e785fd99d230081c3d4527ecc3f485e6be15cf6ba00fd1154d24'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_322001-322912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_322001-322912.nc' 'SHA256' 'f3a4fc6c3df7efc18f5866685685b642bf53f34e384dbe3c286eab21f2a22a4e'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_323001-323912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_323001-323912.nc' 'SHA256' 'b6d9eb7d3af575b63d83d161bb16a86693249f595dc6772a675d110abf6a0b99'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_324001-324912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_324001-324912.nc' 'SHA256' 'bef35eb4b1293730b6c0642e4ea8698d757820a20e54b24e21bd212c8c5c1fd3'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_325001-325912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_325001-325912.nc' 'SHA256' '4f11000cced1368babd942e0461cf157bdbb42fbcb8af89da3c65a63fa2d96b0'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_326001-326912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_326001-326912.nc' 'SHA256' '71fca455988cd94b74d85cf196a09715588c37b9c6e32a4a8f53c29f93babe06'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_327001-327912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_327001-327912.nc' 'SHA256' 'c25ec049e706268d8f2c403d41f252515530ebf3a3f495ef6978752ae8dcdd16'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_328001-328912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_328001-328912.nc' 'SHA256' '1ec415a98c969495dd31b678e291b0032ff77d5e0ce16c7e88ed90b3cd87912a'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_329001-329912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_329001-329912.nc' 'SHA256' 'dc6769f0592d109bc3a043c1bd4c02570073c1be87c271eea7c065955453efa4'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_330001-330912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_330001-330912.nc' 'SHA256' 'd6e413803233d61a9d231a8b6677479ff0cc31730726ae23e7da26bb2b597cd1'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_331001-331912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_331001-331912.nc' 'SHA256' 'a7ad9a8195e95cb48a06db68fad34a29a53494d70004a8ea623f6ab0140cef19'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_332001-332912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_332001-332912.nc' 'SHA256' 'd1cf3e612e7bfa5d9346d2e22c6aa7b27b7c7e00c90b1b389d8530cd3b1baba5'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_333001-333912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_333001-333912.nc' 'SHA256' '4a884196e1496ca1280391f1e3bad2419cb6df4677201650705b3cc0e217553e'
'1pctCO2/MIROC6/r1i1p1f1/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_334001-334912.nc' 'http://esgf-data2.diasjp.net/thredds/fileServer/esg_dataroot/CMIP6/CMIP/MIROC/MIROC6/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190912/dms_AERmon_MIROC6_1pctCO2_r1i1p1f1_gn_334001-334912.nc' 'SHA256' '5e3cd7567abb73f44599318c4ddaf089c8a92feb8915b4a798d54afda024eafe'
'1pctCO2/HadGEM3-GC31-LL/r1i1p1f3/dms_AERmon_HadGEM3-GC31-LL_1pctCO2_r1i1p1f3_gn_185001-189912.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/MOHC/HadGEM3-GC31-LL/1pctCO2/r1i1p1f3/AERmon/dms/gn/v20190620/dms_AERmon_HadGEM3-GC31-LL_1pctCO2_r1i1p1f3_gn_185001-189912.nc' 'SHA256' '6487e042c81fd0f500980ebd6ef0d10466be9803be1dd90d76abaf1039b52288'
'1pctCO2/HadGEM3-GC31-LL/r1i1p1f3/dms_AERmon_HadGEM3-GC31-LL_1pctCO2_r1i1p1f3_gn_190001-194912.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/MOHC/HadGEM3-GC31-LL/1pctCO2/r1i1p1f3/AERmon/dms/gn/v20190620/dms_AERmon_HadGEM3-GC31-LL_1pctCO2_r1i1p1f3_gn_190001-194912.nc' 'SHA256' '6429121b605cce2953e9038a07246ceca95068dbd2fcc0353acaecf54f155b59'
'1pctCO2/HadGEM3-GC31-LL/r1i1p1f3/dms_AERmon_HadGEM3-GC31-LL_1pctCO2_r1i1p1f3_gn_195001-199912.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/MOHC/HadGEM3-GC31-LL/1pctCO2/r1i1p1f3/AERmon/dms/gn/v20190620/dms_AERmon_HadGEM3-GC31-LL_1pctCO2_r1i1p1f3_gn_195001-199912.nc' 'SHA256' 'ae74cb52a625f53077246593798510d9cb33f62a5675cc6de97edf3a91091bf1'
'1pctCO2/UKESM1-0-LL/r1i1p1f2/dms_AERmon_UKESM1-0-LL_1pctCO2_r1i1p1f2_gn_185001-189912.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/MOHC/UKESM1-0-LL/1pctCO2/r1i1p1f2/AERmon/dms/gn/v20190406/dms_AERmon_UKESM1-0-LL_1pctCO2_r1i1p1f2_gn_185001-189912.nc' 'SHA256' '43ea6169c7ff02108f1c7abcbe8619bb76d14a0733019f36f4133d56e1b24815'
'1pctCO2/UKESM1-0-LL/r1i1p1f2/dms_AERmon_UKESM1-0-LL_1pctCO2_r1i1p1f2_gn_190001-194912.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/MOHC/UKESM1-0-LL/1pctCO2/r1i1p1f2/AERmon/dms/gn/v20190406/dms_AERmon_UKESM1-0-LL_1pctCO2_r1i1p1f2_gn_190001-194912.nc' 'SHA256' 'c94ce042d9988b3f0c3aa4078fd5a923dccef8eb0d7eed373277fe85a605ac2e'
'1pctCO2/UKESM1-0-LL/r1i1p1f2/dms_AERmon_UKESM1-0-LL_1pctCO2_r1i1p1f2_gn_195001-199912.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/MOHC/UKESM1-0-LL/1pctCO2/r1i1p1f2/AERmon/dms/gn/v20190406/dms_AERmon_UKESM1-0-LL_1pctCO2_r1i1p1f2_gn_195001-199912.nc' 'SHA256' '96c9561db4e24ab9b9373eb93063a029726923170870abdbe42cf6eec640c540'
'1pctCO2/CESM2-WACCM/r1i1p1f1/dms_AERmon_CESM2-WACCM_1pctCO2_r1i1p1f1_gn_000101-004912.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/NCAR/CESM2-WACCM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190425/dms_AERmon_CESM2-WACCM_1pctCO2_r1i1p1f1_gn_000101-004912.nc' 'SHA256' 'e0c183baea85dd3d597be750fb7131ec7aaba67a7a52f7b958e7e9cd37e7d261'
'1pctCO2/CESM2-WACCM/r1i1p1f1/dms_AERmon_CESM2-WACCM_1pctCO2_r1i1p1f1_gn_005001-009912.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/NCAR/CESM2-WACCM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190425/dms_AERmon_CESM2-WACCM_1pctCO2_r1i1p1f1_gn_005001-009912.nc' 'SHA256' 'dcb38dd72e0361a5f03d8592938aa07de97542e621923bd5075bfc8b7e60550f'
'1pctCO2/CESM2-WACCM/r1i1p1f1/dms_AERmon_CESM2-WACCM_1pctCO2_r1i1p1f1_gn_010001-015012.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/NCAR/CESM2-WACCM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190425/dms_AERmon_CESM2-WACCM_1pctCO2_r1i1p1f1_gn_010001-015012.nc' 'SHA256' 'e349263838a48e661ef3b749deb2e3c1408c79e945a3c8ce95225b99f27f813f'
'1pctCO2/CESM2/r1i1p1f1/dms_AERmon_CESM2_1pctCO2_r1i1p1f1_gn_000101-005012.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/NCAR/CESM2/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190425/dms_AERmon_CESM2_1pctCO2_r1i1p1f1_gn_000101-005012.nc' 'SHA256' '2b751a7ca1d00dfbe137c575a41ad0ef917471d5f035e266798694c5b43783e4'
'1pctCO2/CESM2/r1i1p1f1/dms_AERmon_CESM2_1pctCO2_r1i1p1f1_gn_005101-010012.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/NCAR/CESM2/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190425/dms_AERmon_CESM2_1pctCO2_r1i1p1f1_gn_005101-010012.nc' 'SHA256' 'e7b9f2de5e97e1a659d0aaa8a015b2e9c8529b6f26db8524c47d9be6f7ae1187'
'1pctCO2/CESM2/r1i1p1f1/dms_AERmon_CESM2_1pctCO2_r1i1p1f1_gn_010101-015012.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/NCAR/CESM2/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190425/dms_AERmon_CESM2_1pctCO2_r1i1p1f1_gn_010101-015012.nc' 'SHA256' '8c2e337d913dee069c6a62d514a3e1aad26b7ebfb8b48e56a349cda472f75498'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_000101-001012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_000101-001012.nc' 'SHA256' 'f258a5fde9d1245d441460324b31f0f85ea251f807d16633202f8e7aba27c186'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_001101-002012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_001101-002012.nc' 'SHA256' 'cf557f696096bb6b28a8359d4dea9296fefe6a44b0abebcf0b9d4f1fd784995c'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_002101-003012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_002101-003012.nc' 'SHA256' '4d4223e46e32f2ae352dc8af0e4392320f85a76632d612b90decc9083346887a'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_003101-004012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_003101-004012.nc' 'SHA256' '4927078424f633dbdc335a4c3c79401f8a4740e1ad38d499a0db63be1570faa3'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_004101-005012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_004101-005012.nc' 'SHA256' '93ee4f73fc56b9562e83324961907360fa45b7c377fb2ad7991d0205e37b0671'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_005101-006012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_005101-006012.nc' 'SHA256' 'd78782ca673a87dd3e903fa53d73b9c3189184409e484ab336df9173104e26e9'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_006101-007012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_006101-007012.nc' 'SHA256' 'b7967d853c918aa5c49056bb0424979fcab4bb88f8356fd7d3cd0517e36b17e4'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_007101-008012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_007101-008012.nc' 'SHA256' 'a59f555c1f9f56bc5d357d7a7be549bb92a27233e94e010acae22f433ecfd6b2'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_008101-009012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_008101-009012.nc' 'SHA256' '72590d369202220d641a4a2ee31c83b9cbbbdae9348de12cf46748a64c3e82f2'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_009101-010012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_009101-010012.nc' 'SHA256' 'e3b982183a2b3542efb1084cbc5c1138741cb3bf3f8cf640ed69455f113763a8'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_010101-011012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_010101-011012.nc' 'SHA256' '1b56170c0c4fa9cfeca2c7383096fa4cce1d412da39c77ce4a45e9920eb17c41'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_011101-012012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_011101-012012.nc' 'SHA256' 'ed191c1087aad2004ccc4addbfae3a029cff64f5bfdf5f5c7aa6ae001d0bd115'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_012101-013012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_012101-013012.nc' 'SHA256' 'fed9525a6500a497f7cfe8da3c0b1f73f1295aedeb4ddc9f76ead4bafce97e03'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_013101-014012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_013101-014012.nc' 'SHA256' 'ced5ca5c22cba052a28ae7b0eb2df14729afae52e7f85f13b9231b437ba6efbf'
'1pctCO2/NorESM2-LM/r1i1p1f1/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_014101-015012.nc' 'http://noresg.nird.sigma2.no/thredds/fileServer/esg_dataroot/cmor/CMIP6/CMIP/NCC/NorESM2-LM/1pctCO2/r1i1p1f1/AERmon/dms/gn/v20190815/dms_AERmon_NorESM2-LM_1pctCO2_r1i1p1f1_gn_014101-015012.nc' 'SHA256' 'beada2d153b88f3c1ab278c818054ac8373c85c6f46615b5542e8c51f4952979'
'1pctCO2/GFDL-CM4/r1i1p1f1/dms_AERmon_GFDL-CM4_1pctCO2_r1i1p1f1_gr1_000101-010012.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/NOAA-GFDL/GFDL-CM4/1pctCO2/r1i1p1f1/AERmon/dms/gr1/v20180701/dms_AERmon_GFDL-CM4_1pctCO2_r1i1p1f1_gr1_000101-010012.nc' 'SHA256' 'a40e95a8f869d623a301abba8140a841738adcc702956e97e62bcc4fabfa8b58'
'1pctCO2/GFDL-CM4/r1i1p1f1/dms_AERmon_GFDL-CM4_1pctCO2_r1i1p1f1_gr1_010101-015012.nc' 'http://aims3.llnl.gov/thredds/fileServer/css03_data/CMIP6/CMIP/NOAA-GFDL/GFDL-CM4/1pctCO2/r1i1p1f1/AERmon/dms/gr1/v20180701/dms_AERmon_GFDL-CM4_1pctCO2_r1i1p1f1_gr1_010101-015012.nc' 'SHA256' '10bca0f1140a3e25f8355bdad7f8f82655375b9709bf013f39d65f498dfbd0c8'
EOF--dataset.file.url.chksum_type.chksum
)"

# ESG_HOME should point to the directory containing ESG credentials.
#   Default is $HOME/.esg
ESG_HOME=${ESG_HOME:-$HOME/.esg}
[[ -d $ESG_HOME ]] || mkdir -p $ESG_HOME

ESG_CREDENTIALS=${X509_USER_PROXY:-$ESG_HOME/credentials.pem}
ESG_CERT_DIR=${X509_CERT_DIR:-$ESG_HOME/certificates}
MYPROXY_STATUS=$HOME/.MyProxyLogon
COOKIE_JAR=$ESG_HOME/cookies
MYPROXY_GETCERT=$ESG_HOME/getcert.jar
CERT_EXPIRATION_WARNING=$((60 * 60 * 8))   #Eight hour (in seconds)

WGET_TRUSTED_CERTIFICATES=$ESG_HOME/certificates


# Configure checking of server SSL certificates.
#   Disabling server certificate checking can resolve problems with myproxy
#   servers being out of sync with datanodes.
CHECK_SERVER_CERT=${CHECK_SERVER_CERT:-Yes}

check_os() {
    local os_name=$(uname | awk '{print $1}')
    case ${os_name} in
        Linux)
            ((debug)) && echo "Linux operating system detected"
            LINUX=1
            MACOSX=0
            ;;
        Darwin)
            ((debug)) && echo "Mac OS X operating system detected"
            LINUX=0
            MACOSX=1
            ;;
        *)
            echo "Unrecognized OS [${os_name}]"
            return 1
            ;;
    esac
    return 0
}

#taken from http://stackoverflow.com/a/4025065/1182464
vercomp () {
    if [[ $1 == $2 ]]
    then
        return 0
    fi
    local IFS=.
    local i ver1=($1) ver2=($2)
    # fill empty fields in ver1 with zeros
    for ((i=${#ver1[@]}; i<${#ver2[@]}; i++))
    do
        ver1[i]=0
    done
    for ((i=0; i<${#ver1[@]}; i++))
    do
        if [[ -z ${ver2[i]} ]]
        then
            # fill empty fields in ver2 with zeros
            ver2[i]=0
        fi
        if ((10#${ver1[i]} > 10#${ver2[i]}))
        then
            return 1
        fi
        if ((10#${ver1[i]} < 10#${ver2[i]}))
        then
            return 2
        fi
    done
    return 0
}

check_commands() {
    #check wget
    local MIN_WGET_VERSION=1.10
    vercomp $(wget -V | sed -n 's/^.* \([1-9]\.[0-9.]*\) .*$/\1/p') $MIN_WGET_VERSION
    case $? in
        2) #lower
            wget -V
            echo
            echo "** ERROR: wget version is too old. Use version $MIN_WGET_VERSION or greater. **" >&2
            exit 1
    esac
}

usage() {
    echo "Usage: $(basename $0) [flags] [openid] [username]"
    echo "Flags is one of:"
    sed -n '/^while getopts/,/^done/  s/^\([^)]*\)[^#]*#\(.*$\)/\1 \2/p' $0
    echo
    echo "This command stores the states of the downloads in .$0.status"
    echo "For more information check the website: http://esgf.org/wiki/ESGF_wget"
}

#defaults
debug=0
clean_work=1

#parse flags
while getopts ':c:pfF:o:w:isuUndvqhHI:T' OPT; do
    case $OPT in
        H) skip_security=1 && use_http_sec=1;; #       : Authenticate with OpenID (username,) and password, without the need for a certificate.
        T) force_TLSv1=1;;              #       : Forces wget to use TLSv1. 
        c) ESG_CREDENTIALS="$OPTARG";;  #<cert> : use this certificate for authentication.
        f) force=1;;                    #       : force certificate retrieval (defaults to only once per day); for certificate-less authentication (see -H option), this flag will force login and refresh cookies.
        F) input_file="$OPTARG";;       #<file> : read input from file instead of the embedded one (use - to read from stdin)
        o) openId="$OPTARG";;           #<openid>: Provide OpenID instead of interactively asking for it.
        I) username_supplied="$OPTARG";;    #<user_id> : Explicitly set user ID.  By default, the user ID is extracted from the last component of the OpenID URL.  Use this flag to override this behaviour.                   
        w) output="$OPTARG";;           #<file> : Write embedded files into a file and exit
        i) insecure=1;;                 #       : set insecure mode, i.e. don't check server certificate
        s) skip_security=1 && use_cookies_for_http_basic_auth_start=1;;            #       : completely skip security. It will only work if the accessed data is not secured at all. -- works only if the accessed data is unsecured or a certificate exists or cookies are saved (latter applies to -H option only).
        u) update=1;;                   #       : Issue the search again and see if something has changed.
        U) update_files=1;;             #       : Update files from server overwriting local ones (detect with -u)
        n) dry_run=1;;                  #       : Don't download any files, just report.
        p) clean_work=0;;               #       : preserve data that failed checksum
        d) verbose=1;debug=1;;          #       : display debug information
        v) verbose=1;;                  #       : be more verbose
        q) quiet=1;;                    #       : be less verbose
        h) usage && exit 0;;            #       : displays this help
        \?) echo "Unknown option '$OPTARG'" >&2 && usage && exit 1;;
        \:) echo "Missing parameter for flag '$OPTARG'" >&2 && usage && exit 1;;
    esac
done
shift $(($OPTIND - 1))

#setup input as desired by the user
if [[ "$input_file" ]]; then
    if [[ "$input_file" == '-' ]]; then
        download_files="$(cat)" #read from STDIN
        exec 0</dev/tty #reopen STDIN as cat closed it
    else
        download_files="$(cat $input_file)" #read from file
    fi
fi

#if -w (output) was selected write file and finish:
if [[ "$output" ]]; then
    #check the file
    if [[ -f "$output" ]]; then
        read -p "Overwrite existing file $output? (y/N) " answ
        case $answ in y|Y|yes|Yes);; *) echo "Aborting then..."; exit 0;; esac
    fi
    echo "$download_files">$output
    exit
fi


#assure we have everything we need
check_commands

if ((update)); then
    echo "Checking the server for changes..."
    new_wget="$(wget "$search_url" -qO -)"
    compare_cmd="grep -vE '^(# Generated by|# Search URL|search_url=)'"
    if diff -q <(eval $compare_cmd<<<"$new_wget") <(eval $compare_cmd $0) >/dev/null; then
        echo "No changes detected."
    else
        echo "Wget was changed. Dowloading. (old renamed to $0.old.#N)"
        counter=0
        while [[ -f $0.old.$counter ]]; do ((counter++)); done
        mv $0 $0.old.$counter
        echo "$new_wget" > $0
    fi
    exit 0      
fi


##############################################################################
check_java() {
    if ! type java >& /dev/null; then
        echo "Java could not be found." >&2
        return 1
    fi
    if java -version 2>&1|grep openjdk >/dev/null; then
        openjdk=1;
    else
        openjdk=0;
    fi
    jversion=($(jversion=$(java -version 2>&1 | awk '/version/ {gsub("\"","");print $3}'); echo ${jversion//./ }))
    mVer=${jversion[1]}
    if [ $openjdk -eq 1 ]; then
        mVer=${jversion[0]}
        if ((mVer<5)); then
            echo "Openjdk detected. Version 9+ is required for retrieving the certificate." >&2
            echo "Current version seems older: $(java -version | head -n1) " >&2
            return 1
        fi
    else
    
        if ((mVer<5)); then
            echo "Java version 1.5+ is required for retrieving the certificate." >&2
            echo "Current version seems older: $(java -version | head -n1) " >&2
            return 1
        fi
    fi
}

check_myproxy_logon() {
    if ! type myproxy-logon >& /dev/null; then
	echo "myproxy-logon could not be found." >&2
	return 1
    fi
    echo "myproxy-logon found" >&2
}

proxy_to_java() {
    local proxy_user proxy_pass proxy_server proxy_port
    eval $(sed 's#^\(https\?://\)\?\(\([^:@]*\)\(:\([^@]*\)\)\?@\)\?\([^:/]*\)\(:\([0-9]*\)\)\?.*#proxy_user=\3;proxy_pass=\5;proxy_server=\6;proxy_port=\8#'<<<$http_proxy)
    local JAVA_PROXY=
    [[ "$proxy_server" ]] && JAVA_PROXY=$JAVA_PROXY" -Dhttp.proxyHost=$proxy_server"
    [[ "$proxy_port" ]] && JAVA_PROXY=$JAVA_PROXY" -Dhttp.proxyPort=$proxy_port"
    eval $(sed 's#^\(https\?://\)\?\(\([^:@]*\)\(:\([^@]*\)\)\?@\)\?\([^:/]*\)\(:\([0-9]*\)\)\?.*#proxy_user=\3;proxy_pass=\5;proxy_server=\6;proxy_port=\8#'<<<$https_proxy)
    [[ "$proxy_server" ]] && JAVA_PROXY=$JAVA_PROXY" -Dhttps.proxyHost=$proxy_server"
    [[ "$proxy_port" ]] && JAVA_PROXY=$JAVA_PROXY" -Dhttps.proxyPort=$proxy_port"
    
    echo "$JAVA_PROXY"
}

# get certificates from github
get_certificates() {
    # don't if this was already done today
    [[ -z $force && "$(find $ESG_CERT_DIR -type d -mtime -1 2>/dev/null)" ]] && return 0
    echo -n "Retrieving Federation Certificates..." >&2

    if ! wget -O $ESG_HOME/esg-truststore.ts --no-check-certificate https://github.com/ESGF/esgf-dist/raw/master/installer/certs/esg-truststore.ts; then
        echo "Could not fetch esg-truststore";
        return 1
    fi
    
    if ! wget --no-check-certificate https://raw.githubusercontent.com/ESGF/esgf-dist/master/installer/certs/esg_trusted_certificates.tar -O - -q | tar x -C $ESG_HOME; then
        #certificates tarred into esg_trusted_certificates. (if it breaks, let the user know why
        wget --no-check-certificate https://raw.githubusercontent.com/ESGF/esgf-dist/master/installer/certs/esg_trusted_certificates.tar
        echo "Could't update certs!" >&2
        return 1
    else
        #if here everythng went fine. Replace old cert with this ones    
        [[ -d $ESG_CERT_DIR ]] && rm -r $ESG_CERT_DIR || mkdir -p $(dirname $ESG_CERT_DIR)
        mv $ESG_HOME/esg_trusted_certificates $ESG_CERT_DIR
        touch $ESG_CERT_DIR
        echo "done!" >&2
    fi

}

# Retrieve ESG credentials
unset pass
get_credentials() {
    if check_java
    then
	use_java=1
    else	
	use_java=0
	echo "No suitable java for obtaining certificate - checking for myproxy-logon instead" >&2
	check_myproxy_logon || exit 1
    fi
    #get all certificates
    get_certificates

    if [[ -z "$(find $MYPROXY_GETCERT -type f -mtime -1 2>/dev/null)" ]]; then
        echo -n "(Downloading $MYPROXY_GETCERT... "
        mkdir -p $(dirname $MYPROXY_GETCERT)
        if wget -q --no-check-certificate https://raw.githubusercontent.com/ESGF/esgf-dist/master/installer/certs/getcert.jar -O $MYPROXY_GETCERT;then
            echo 'done)'
            touch $MYPROXY_GETCERT
        else
            echo 'failed)'
        fi
    fi

    #if the user already defined one, use it
    if [[ -z $openId ]]; then
        #try to parse the last valid value if any
        [[ -f "$MYPROXY_STATUS" ]] && openId=$(awk -F= '/^OpenID/ {gsub("\\\\", ""); print $2}' $MYPROXY_STATUS)
        if [[ -z $openId ]]; then
            #no OpenID, we need to ask the user
            echo -n "Please give your OpenID (Example: https://myserver/example/username) ? "
        else
            #Allow the user to change it if desired
            echo -n "Please give your OpenID (hit ENTER to accept default: $openId)? "
        fi
        read -e
        [[ "$REPLY" ]] && openId="$REPLY"
    else
        ((verbose)) && echo "Using user defined OpenID $openId (to change use -o <open_id>)"
    fi

    if grep -q ceda.ac.uk <<<$openId; then
        username=${openId##*/}
        echo -n "Please give your username if different [$username]: "
        read -e
        [[ "$REPLY" ]] && username="$REPLY"
    fi
    


    if [ $use_java -eq 1 ]
    then
        local args=
        #get password
	[[ ! "$pass" ]] && read -sp "MyProxy Password? " pass
        [[ "$openId" ]] && args=$args" --oid $openId"
        [[ "$pass" ]] && args=$args" -P $pass"
        [[ "$username" ]] && args=$args" -l $username"
        
        echo -n $'\nRetrieving Credentials...' >&2
        if ! java $(proxy_to_java) -jar $MYPROXY_GETCERT $args --ca-directory $ESG_CERT_DIR --output $ESG_CREDENTIALS ; then        
            echo "Certificate could not be retrieved"
            exit 1
        fi
        echo "done!" >&2
    else
        args=`openid_to_myproxy_args $openId $username` || exit 1
        if ! myproxy-logon $args -b -o $ESG_CREDENTIALS
	then
            echo "Certificate could not be retrieved"
	    exit 1
        fi
	cp $HOME/.globus/certificates/* $ESG_CERT_DIR/	
    fi
}

openid_to_myproxy_args() {
  python - <<EOF || exit 1
import sys
import re
import xml.etree.ElementTree as ET
import urllib2
openid = "$1"
username = "$2" or re.sub(".*/", "", openid)
e = ET.parse(urllib2.urlopen(openid))
servs = [el for el in e.getiterator() if el.tag.endswith("Service")]
for serv in servs:
    servinfo = dict([(re.sub(".*}", "", c.tag), c.text)
                     for c in serv.getchildren()])
    try:
        if servinfo["Type"].endswith("myproxy-service"):
            m = re.match("socket://(.*):(.*)", servinfo["URI"])
            if m:
                host = m.group(1)
                port = m.group(2)
                print "-s %s -p %s -l %s" % (host, port, username)
                break
    except KeyError:
        continue
else:
    sys.stderr.write("myproxy service could not be found\n")
    sys.exit(1)
EOF
}

# check the certificate validity
check_cert() {
    if [[ ! -f "$ESG_CERT" || $force ]]; then
        #not there, just get it
        get_credentials
    elif which openssl &>/dev/null; then
        #check openssl and certificate
        if ! openssl x509 -checkend $CERT_EXPIRATION_WARNING -noout -in $ESG_CERT 2>/dev/null; then
            echo "The certificate expires in less than $((CERT_EXPIRATION_WARNING / 60 / 60)) hour(s). Renewing..."
            get_credentials
        else
            #ok, certificate is fine
            return 0
        fi
    fi
}

#
# Detect ESG credentials
#
find_credentials() {

    #is X509_USER_PROXY or $HOME/.esg/credential.pem
    if [[ -f "$ESG_CREDENTIALS" ]]; then
        # file found, proceed.
        ESG_CERT="$ESG_CREDENTIALS"
        ESG_KEY="$ESG_CREDENTIALS"
    elif [[ -f "$X509_USER_CERT" && -f "$X509_USER_KEY" ]]; then
        # second try, use these certificates.
        ESG_CERT="$X509_USER_CERT"
        ESG_KEY="$X509_USER_KEY"
    else
        # If credentials are not present, just point to where they should go 
        echo "No ESG Credentials found in $ESG_CREDENTIALS" >&2
            ESG_CERT="$ESG_CREDENTIALS"
            ESG_KEY="$ESG_CREDENTIALS"
            #they will be retrieved later one
    fi


    #chek openssl and certificate
    if (which openssl &>/dev/null); then
        if ( openssl version | grep 'OpenSSL 1\.0' ); then
            echo '** WARNING: ESGF Host certificate checking might not be compatible with OpenSSL 1.0+'
        fi
        check_cert || { (($?==1)); exit 1; }
    fi
    
    if [[ $CHECK_SERVER_CERT == "Yes" ]]; then
        [[ -d "$ESG_CERT_DIR" ]] || { echo "CA certs not found. Aborting."; exit 1; }
        PKI_WGET_OPTS="--ca-directory=$ESG_CERT_DIR"
    fi

    #some wget version complain if there's no file present
    [[ -f $COOKIE_JAR ]] || touch $COOKIE_JAR

    PKI_WGET_OPTS="$PKI_WGET_OPTS --certificate=$ESG_CERT --private-key=$ESG_KEY --save-cookies=$COOKIE_JAR --load-cookies=$COOKIE_JAR --ca-certificate=$ESG_CERT"

}

check_chksum() {
    local file="$1"
    local chk_type=$2
    local chk_value=$3
    local local_chksum=Unknown

    case $chk_type in
        md5) local_chksum=$(md5sum_ $file | cut -f1 -d" ");;
        sha256) local_chksum=$(sha256sum_ $file|awk '{print $1}'|cut -d ' ' -f1);;
        *) echo "Can't verify checksum." && return 0;;
    esac

    #verify
    ((debug)) && echo "local:$local_chksum vs remote:$chk_value" >&2
    echo $local_chksum
}

#Our own md5sum function call that takes into account machines that don't have md5sum but do have md5 (i.e. mac os x)
md5sum_() {
    hash -r
    if type md5sum >& /dev/null; then
        echo $(md5sum $@)
    else
        echo $(md5 $@ | sed -n 's/MD5[ ]*\(.*\)[^=]*=[ ]*\(.*$\)/\2 \1/p')
    fi
}

#Our own sha256sum function call that takes into account machines that don't have sha256sum but do have sha2 (i.e. mac os x)
sha256sum_() {
    hash -r
    if type sha256sum >& /dev/null; then
        echo $(sha256sum $@)
    elif type shasum >& /dev/null; then
        echo $(shasum -a 256 $@)
    else
        echo $(sha2 -q -256 $@)
    fi
}

get_mod_time_() {
    if ((MACOSX)); then
        #on a mac modtime is stat -f %m <file>
        echo "$(stat -f %m $@)"
    else
        #on linux (cygwin) modtime is stat -c %Y <file>
        echo "$(stat -c %Y $@)"
    fi
    return 0;
}

remove_from_cache() {
    local entry="$1"
    local tmp_file="$(grep -ve "^$entry" "$CACHE_FILE")"
    echo "$tmp_file" > "$CACHE_FILE"
    unset cached
}

#Download data from node using cookies and not certificates.
download_http_sec()
{
  #The data to be downloaded.
  data=" $url"
  filename="$file"  

  #Wget args.
  if ((insecure)) 
  then
   wget_args=" --no-check-certificate --cookies=on  --keep-session-cookies --save-cookies $COOKIES_FOLDER/wcookies.txt " 
  else
   wget_args=" --ca-directory=$WGET_TRUSTED_CERTIFICATES --cookies=on --keep-session-cookies --save-cookies $COOKIES_FOLDER/wcookies.txt "  
  fi 

  if ((use_cookies_for_http_basic_auth_start)) || ((use_cookies_for_http_basic_auth)) 
  then
   wget_args=" $wget_args"" --load-cookies $COOKIES_FOLDER/wcookies.txt"    
  fi
  
  if((force_TLSv1))
  then
   wget_args=" $wget_args"" --secure-protocol=TLSv1 "
  fi
  
  
  if [[ ! -z "$ESGF_WGET_OPTS" ]]
  then
    wget_args="$wget_args $ESGF_WGET_OPTS"
  fi  
  

  #use cookies for the next downloads
  use_cookies_for_http_basic_auth=1;
   
  #Debug message.
  if  ((debug))
  then
   echo -e "\nExecuting:\n"
   echo -e "wget $wget_args $data\n"
  fi


  #Try to download the data. 
  command="wget $wget_args -O $filename $data"
  http_resp=$(eval $command  2>&1) 
  cmd_exit_status="$?"
  
  if ((debug))
  then
   echo -e "\nHTTP response:\n $http_resp\n"
  fi
      
  #Extract orp service from url ?
  #Evaluate response.
  #redirects=$(echo "$http_resp" | egrep -c ' 302 ')
  #(( "$redirects" == 1 )) && 
  if  echo "$http_resp" | grep -q "/esg-orp/"      
  then
   urls=$(echo "$http_resp" | egrep -o 'https://[^ ]+' | cut -d'/' -f 3)
   orp_service=$(echo "$urls" | tr '\n' ' ' | cut -d' ' -f 2)


   #Use cookies for transaction with orp.
   wget_args=" $wget_args"" --load-cookies $COOKIES_FOLDER/wcookies.txt"    
   
   #Download data using either http basic auth or http login form.
   if [[ "$openid_c" == */openid/  || "$openid_c" == */openid ]]
   then
    download_http_sec_open_id
   else
    download_http_sec_decide_service
   fi
  else  
   if    echo "$http_resp" | grep -q "401 Unauthorized"  \
      || echo "$http_resp" | grep -q "403: Forbidden"  \
      || echo "$http_resp" | grep -q "Connection timed out."  \
      || echo "$http_resp" | grep -q "no-check-certificate"  \
      || (( $cmd_exit_status != 0 ))      
   then 
    echo "ERROR : http request to OpenID Relying Party service failed."
    failed=1
   fi
  fi
}


#Function that decides which implementaion of idp to use.
download_http_sec_decide_service()
{
  #find claimed id

  pos=$(echo "$openid_c" | egrep -o '/' | wc -l)
  username_c=$(echo "$openid_c"  | cut -d'/' -f "$(($pos + 1))")
  esgf_uri=$(echo "$openid_c" | egrep -o '/esgf-idp/openid/')

  host=$(echo "$openid_c"  | cut -d'/' -f 3)
  #test ceda first.

  if [[ -z "$esgf_uri" ]]
  then
   openid_c_tmp="https://""$host""/openid/"
  else
   openid_c_tmp="https://""$host""/esgf-idp/openid/" 
  fi

  command="wget "$openid_c_tmp" --no-check-certificate ${force_TLSv1:+--secure-protocol=TLSv1} -O-"
        
  if [[ ! -z "$ESGF_WGET_OPTS" ]]
  then
   command="$command $ESGF_WGET_OPTS"
  fi  
          
  #Debug message.
  if  ((debug))
  then
   echo -e "\nExecuting:\n"
   echo -e "$command\n"
  fi
            

  #Execution of command.
  http_resp=$(eval $command  2>&1)
  cmd_exit_status="$?"
  
  
  if ((debug))
  then
   echo -e "\nHTTP response:\n $http_resp\n"
  fi 
  

  if    echo "$http_resp" | grep -q "[application/xrds+xml]"  \
     && echo "$http_resp" | grep -q "200 OK"  \
     && (( cmd_exit_status == 0 ))       
  then
   openid_c=$openid_c_tmp
   download_http_sec_open_id
  else
   if [[ -z "$esgf_uri" ]]
   then
    echo "ERROR : HTTP request to OpenID Relying Party service failed."
    failed=1
   else
   download_http_sec_cl_id
   fi
  fi
}


download_http_sec_retry()
{
  echo -e "\nRetrying....\n"
  #Retry in case that last redirect did not work, this happens with older version of wget.
  command="wget $wget_args $data"
      
  #Debug message.
  if  ((debug))
  then
   echo -e "Executing:\n"
   echo -e "$command\n"
  fi   
   
  http_resp=$(eval $command  2>&1) 
  cmd_exit_status="$?"

  if ((debug))
  then
   echo -e "\nHTTP response:\n $http_resp\n"
  fi
   
  if    echo "$http_resp" | grep -q "401 Unauthorized"  \
     || echo "$http_resp" | grep -q "403: Forbidden"  \
     || echo "$http_resp" | grep -q "Connection timed out."  \
     || echo "$http_resp" | grep -q "no-check-certificate"  \
     || (( $cmd_exit_status != 0 ))      
  then 
   echo -e "\nERROR : Retry failed.\n"
   #rm "$filename"
   failed=1
  fi #if retry failed.
}

#Function for downloading data using the claimed id.
download_http_sec_cl_id()
{
  #Http request for sending openid to the orp service.
  command="wget --post-data \"openid_identifier=$openid_c&rememberOpenid=on\"  $wget_args -O- https://$orp_service/esg-orp/j_spring_openid_security_check.htm "

  #Debug message.
  if  ((debug))
  then
   echo -e "Executing:\n"
   echo -e "wget $command\n"
  fi 
  
  
  #Execution of command.
  http_resp=$(eval $command  2>&1)
  cmd_exit_status="$?"

  
  if ((debug))
  then
   echo -e "\nHTTP response:\n $http_resp\n"
  fi 
    
  
  #Extract orp service from openid ?
  #Evaluate response.If redirected to idp service send the credentials.
  #redirects=$(echo "$http_resp" | egrep -c ' 302 ')
  #(( redirects == 2  )) && 
  if  echo "$http_resp" | grep -q "login.htm"  && (( cmd_exit_status == 0 ))   
  then 
  
   urls=$(echo "$http_resp" | egrep -o 'https://[^ ]+' | cut -d'/' -f 3)
   idp_service=$(echo "$urls"  | tr '\n' ' ' | cut -d' ' -f 2) 
      
   command="wget --post-data  password=\"$password_c\" $wget_args ${quiet:+-q} ${quiet:--v} -O $filename https://$idp_service/esgf-idp/idp/login.htm"
   

   #Debug message.
   if  ((debug))
   then
    echo -e "Executing:\n"
    echo -e "wget $command\n"
   fi 

   #Execution of command.
   http_resp=$(eval $command  2>&1)
   cmd_exit_status="$?"
      
   if ((debug))
   then
    echo -e "\nHTTP response:\n $http_resp\n"
   fi 
        
   #Evaluate response. 
   #redirects=$(echo "$http_resp" | egrep -c ' 302 ')
   #(( "$redirects" != 5 )) \ 
   if    echo "$http_resp" | grep -q "text/html"  \
      || echo "$http_resp" | grep -q "403: Forbidden"  \
      || (( cmd_exit_status != 0 ))        
   then 
    rm "$filename"
    download_http_sec_retry
   fi
 
  else
   echo "ERROR : HTTP request to OpenID Provider service failed."
   failed=1
  fi #if redirected to idp.  
}



download_http_sec_open_id()
{
  #Http request for sending openid to the orp web service.
  command="wget --post-data \"openid_identifier=$openid_c&rememberOpenid=on\" --header=\"esgf-idea-agent-type:basic_auth\" --http-user=\"$username_c\" --http-password=\"$password_c\"  $wget_args ${quiet:+-q} ${quiet:--v} -O $filename https://$orp_service/esg-orp/j_spring_openid_security_check.htm "


  #Debug message.
  if  ((debug))
  then
   echo -e "Executing:\n"
   echo -e "$command\n"
  fi 

  #Execution of command.
  http_resp=$(eval $command  2>&1)
  cmd_exit_status="$?"
  
  
  if ((debug))
  then
   echo -e "\nHTTP response:\n $http_resp\n"
  fi 

  #Evaluate response.
  #redirects=$(echo "$http_resp" | egrep -c ' 302 ')
  #(( "$redirects" != 7 )) ||
  if   echo "$http_resp" | grep -q "text/html"  ||  (( $cmd_exit_status != 0 ))   
  then
   rm "$filename"
   download_http_sec_retry     
  fi #if error during http basic authentication. 
  
}


download() {
    wget="wget ${insecure:+--no-check-certificate} ${quiet:+-q} ${quiet:--v} -c ${force_TLSv1:+--secure-protocol=TLSv1} $PKI_WGET_OPTS"
    
    while read line
    do
        # read csv here document into proper variables
        eval $(awk -F "' '" '{$0=substr($0,2,length($0)-2); $3=tolower($3); print "file=\""$1"\";url=\""$2"\";chksum_type=\""$3"\";chksum=\""$4"\""}' <(echo $line) )

        #Process the file
        echo -n "$file ..."

        #get the cached entry if any.
        cached="$(grep -e "^$file" "$CACHE_FILE")"
        
        #if we have the cache entry but no file, clean it.
        if [[ ! -f $file && "$cached" ]]; then
            #the file was removed, clean the cache
            remove_from_cache "$file"
            unset cached
        fi
        
        #check it wasn't modified
        if [[ -n "$cached" && "$(get_mod_time_ $file)" == $(echo "$cached" | cut -d ' ' -f2) ]]; then
                    if [[ "$chksum" == "$(echo "$cached" | cut -d ' ' -f3)" ]]; then
                echo "Already downloaded and verified"
                continue
            elif ((update_files)); then
                #user want's to overwrite newer files
                rm $file
                remove_from_cache "$file"
                unset cached
            else
                #file on server is different from what we have. 
                echo "WARNING: The remote file was changed (probably a new version is available). Use -U to Update/overwrite"
                continue
            fi
        fi
        unset chksum_err_value chksum_err_count
        
        while : ; do
            # (if we had the file size, we could check before trying to complete)
            echo "Downloading"
            [[ ! -d "$(dirname "$file")" ]] && mkdir -p "$(dirname "$file")"
            if ((dry_run)); then
                #all important info was already displayed, if in dry_run mode just abort
                #No status will be stored
                break
            else
                if ((use_http_sec))
                then
                 download_http_sec
                 if ((failed))
                 then
                  break
                 fi
                else
                 $wget -O "$file" $url || { failed=1; break; }  
                fi                
            fi

            #check if file is there
            if [[ -f $file ]]; then
                ((debug)) && echo file found
                if [[ ! "$chksum" ]]; then
                    echo "Checksum not provided, can't verify file integrity"
                    break
                fi
                result_chksum=$(check_chksum "$file" $chksum_type $chksum)
                if [[ "$result_chksum" != "$chksum" ]]; then
                    echo "  $chksum_type failed!"
                    if ((clean_work)); then
                        if !((chksum_err_count)); then
                                chksum_err_value=$result_chksum
                                chksum_err_count=2
                            elif ((checksum_err_count--)); then
                                if [[ "$result_chksum" != "$chksum_err_value" ]]; then
                                    #this is a real transmission problem
                                    chksum_err_value=$result_chksum
                                    chksum_err_count=2
                                fi
                            else
                                #ok if here we keep getting the same "different" checksum
                                echo "The file returns always a different checksum!"
                                echo "Contact the data owner to verify what is happening."
                                echo
                                sleep 1
                                break
                            fi
                        
                            rm $file
                            #try again
                            echo -n "  re-trying..."
                            continue
                    else
                            echo "  don't use -p or remove manually."
                    fi
                else
                    echo "  $chksum_type ok. done!"
                    echo "$file" $(get_mod_time_ "$file") $chksum >> $CACHE_FILE
                fi
            fi
            #done!
            break
        done
        
        if ((failed)); then
            echo "download failed"
            # most common failure is certificate expiration, so check this
            #if we have the pasword we can retrigger download
            ((!skip_security)) && [[ "$pass" ]] && check_cert
            unset failed
        fi
        
done <<<"$download_files"

}

dedup_cache_() {
    local file=${1:-${CACHE_FILE}}
    ((debug)) && echo "dedup'ing cache ${file} ..."
    local tmp=$(LC_ALL='C' sort  -r -k1,2 $file | awk '!($1 in a) {a[$1];print $0}' | sort -k2,2)
    ((DEBUG)) && echo "$tmp"
    echo "$tmp" > $file
    ((debug)) && echo "(cache dedup'ed)"
}

http_basic_auth_func_info_message()
{
  echo  "********************************************************************************"
  echo  "*                                                                              *"
  echo  "* Note that new functionality to allow authentication without the need for     *"
  echo  "* certificates is available with this version of the wget script.  To enable,  *"
  echo  "* use the \"-H\" option and enter your OpenID and password when prompted:        *"
  echo  "*                                                                              *"
  echo  "* $ "$(basename "$0")" -H [options...]                                     *"
  echo  "*                                                                              *"
  echo  "* For a full description of the available options use the help option:         *"
  echo  "*                                                                              *"
  echo  "* $ "$(basename "$0")" -h                                                  *"
  echo  "*                                                                              *"
  echo  "********************************************************************************"
}

#
# MAIN
#

if ((!use_http_sec))
then 
 http_basic_auth_func_info_message
fi

echo "Running $(basename $0) version: $version"
((verbose)) && echo "we use other tools in here, don't try to user their proposed 'options' directly"
echo "Use $(basename $0) -h for help."$'\n'

((debug)) && cat<<EOF
** Debug info **
ESG_HOME=$ESG_HOME
ESG_CREDENTIALS=$ESG_CREDENTIALS
ESG_CERT_DIR=$ESG_CERT_DIR
** -- ** -- ** -- ** --

EOF


cat <<'EOF-MESSAGE'
There were files with the same name which were requested to be download to the same directory. To avoid overwriting the previous downloaded one they were skipped.
Please use the parameter 'download_structure' to set up unique directories for them.
Script created for 46 file(s)
(The count won't match if you manually edit this file!)



EOF-MESSAGE
sleep 1

check_os
((!skip_security)) && find_credentials

if ((use_http_sec))
then 
     
 if (( ! insecure))
 then 
  get_certificates
 fi

 #Cookies folder.
 COOKIES_FOLDER="$ESG_HOME/wget_cookies"
 
 if (( force ))
 then
  if [ -d $COOKIES_FOLDER ] 
  then
   rm -rf $COOKIES_FOLDER
  fi
 fi

 #Create cookies folder. 
 if [[ ! -d $COOKIES_FOLDER ]] 
 then
  mkdir $COOKIES_FOLDER
 fi
 
 if((! use_cookies_for_http_basic_auth_start))
 then

  #Read openid.
  if [[ ! -z "$openId" ]]
  then
   openid_c="$openId"
  elif ( (("$#" > 1)) || (("$#" == 1)) ) 
  then
   openid_c=$1
  else
   read -p    "Enter your openid : " openid_c
  fi
  
  
  #Read username.
  if [[ ! -z "$username_supplied" ]]
  then
   username_c="$username_supplied"
  elif (("$#" == 2))
  then
   username_c=$2
  elif [[ "$openid_c" == */openid/ || "$openid_c" == */openid ]]
  then
   read -p    "Enter username : " username_c
  fi
  
  #Read password.
  read -s -p "Enter password : " password_c
  echo -e "\n"

 fi #use cookies

fi #use_http_sec 


#do we have old results? Create the file if not
[ ! -f $CACHE_FILE ] && echo "#filename mtime checksum" > $CACHE_FILE && chmod 666 $CACHE_FILE

#clean the force parameter if here (at htis point we already have the certificate)
unset force

download

dedup_cache_


echo "done"
